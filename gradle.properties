kotlin.code.style=official
android.useAndroidX=true
org.gradle.jvmargs=-Xmx4g

# Version stamped onto the SDK library modules and exposed to the tool via the
# `sdkVersion` manifest placeholder. Track the pinned submodule.
sdkGroup=com.thelightphone
sdkVersion=0.0.12
